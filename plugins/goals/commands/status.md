---
description: "Show status of the active goal"
allowed-tools: Bash
---

# Goal Status

Show the current goal's status, including iteration count and precheck results.

Run this command:

```bash
node ${CLAUDE_PLUGIN_ROOT}/scripts/goal-status.mjs
```
