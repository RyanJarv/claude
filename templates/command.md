Perform the following task based on the user's request:

$ARGUMENTS

<!--
  This is a command template for {{PLUGIN_NAME}}.

  Instructions:
  - The filename (minus .md) becomes the command name
  - $ARGUMENTS is replaced with user input after the command name
  - Write clear, specific instructions for what Claude should do
  - Example: /{{PLUGIN_NAME}}:command-name some arguments
-->
